<nav class="navbar navbar-expand-lg bg-dark navbar-dark mb-4">
    <div class="container-fluid">

        <a class="navbar-brand"
           href="{{ isEnglish ? path('app_genillon_nathan_en') : path('app_genillon_nathan') }}">
            Home
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarColor02">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor02">

            <ul class="navbar-nav me-auto">

                <li class="nav-item">
                    <a class="nav-link"
                       href="{{ isEnglish ? path('app_cv_en') : path('app_cv') }}">
                        CV
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link"
                       href="{{ isEnglish ? path('app_portfolio_en') : path('app_portfolio') }}">
                        Portfolio
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link"
                       href="{{ isEnglish ? path('app_En_savoir_plus_en') : path('app_En_savoir_plus') }}">
                        En savoir plus
                    </a>
                </li>

            </ul>

            <ul class="navbar-nav ms-auto">
                {% if isEnglish %}
                    <li class="nav-item">
                        <a class="nav-link btn btn-outline-light ms-2"
                           href="{{ currentPath|replace({'/en': ''}) }}">
                            Français
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link btn btn-outline-light ms-2"
                           href="/en{{ currentPath }}">
                            English
                        </a>
                    </li>
                {% endif %}
            </ul>

        </div>
    </div>
</nav>
